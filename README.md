# Cloudways Deploy Branch

This branch contains the built static site for Cloudways Git deployment.

- Source code lives on `main`.
- Cloudways should deploy this branch into `public_html/`.
- The included `.htaccess` rewrites requests to `index.html` for SPA routing.
